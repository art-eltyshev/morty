{
  "name": "morty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -r build && mkdir -p build && cp -R package.json index.js libs build && cd build && npm i --production && zip -r build.zip index.js libs node_modules && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/art-eltyshev/morty.git"
  },
  "author": "art-eltyshev",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/art-eltyshev/morty/issues"
  },
  "homepage": "https://github.com/art-eltyshev/morty#readme",
  "dependencies": {
    "google-auth-library": "^1.3.2",
    "googleapis": "^27.0.0",
    "telegraf": "^3.16.4"
  }
}
