{
  "name": "morty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build && cp -R package.json index.js libs build && cd build && npm i --production && zip -r build.zip index.js libs node_modules && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/art-eltyshev/morty.git"
  },
  "author": "art-eltyshev",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/art-eltyshev/morty/issues"
  },
  "homepage": "https://github.com/art-eltyshev/morty#readme",
  "dependencies": {
    "telegraf": "^3.16.4"
  },
  "devDependencies": {
    "eslint": "^4.10.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0"
  }
}
